---
- name: Install Dependencies
  apt:
    pkg:
      - apt-transport-https
      - ca-certificates
      - gnupg2
      - software-properties-common
      - curl
      - rpm
      - zip
      - unzip
    state: present
    update_cache: yes